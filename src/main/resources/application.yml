server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_usuarios
    username: root
    password: 123

#con esto se ectualizan las tablas automaticamente
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
